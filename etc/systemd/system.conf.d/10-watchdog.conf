[Manager]
# The SoC hangs hard with no kernel output and nothing recovers it; the board
# sat dead for 9 and 33 hours after the last two. Let the dw_wdt reset it.
RuntimeWatchdogSec=60s
RebootWatchdogSec=5min
