# Gate raspotify on SPOTIFY= in /etc/BlueRelief/bluerelief.conf.
# raspotify.service is owned by apt, so the switch lives in a drop-in.
[Service]
ExecCondition=/usr/local/bin/bluerelief-config check SPOTIFY
