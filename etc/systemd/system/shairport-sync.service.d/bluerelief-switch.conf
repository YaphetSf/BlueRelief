# Gate shairport-sync on AIRPLAY= in /etc/BlueRelief/bluerelief.conf.
# shairport-sync.service is owned by apt, so the switch lives in a drop-in.
[Service]
ExecCondition=/usr/local/bin/bluerelief-config check AIRPLAY
